# Автоматизация тестирования <code>[Conduit](https://demo.realworld.io/ "Conduit")</code> с помощью Cypress

```
 npm install cypress --save-dev
```
Запуск тестов:
```
npm test
```
Запуск в интерактивном режиме:
```
npx cypress open
```